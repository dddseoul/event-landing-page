# AGENTS.md

## 프로젝트 개요와 범위

- 이 저장소는 **DDD Seoul 행사만** 다룬다. 다른 행사 페이지나 예제를 추가하지 않는다.
- 티켓타코 행사 소개에 붙여넣을 정적 HTML과 콘텐츠 원본, 이미지 자산을 관리한다.
- 공식 홈페이지는 `https://dddseoul.kr/`이다.
- 기존 구현은 `ticketaco\2026\12\dddseoul\index.html`을 참고한다. 다음 행사에 날짜, 장소, 후원사까지 그대로 복사하지 않는다.
- 페이지 생성 스킬의 범용 예시보다 이 문서의 DDD Seoul 및 티켓타코 규칙을 우선 적용한다.

## 저장소 구조

```text
ticketaco\
  {YYYY}\
    {MM}\
      {event-slug}\
        index.md
        index.html
        images\
.github\
  skills\
  workflows\
AGENTS.md
README.md
```

- `index.md`: 행사 문구, 확정 정보, 신청 방식의 원본이다.
- `index.html`: CSS와 콘텐츠를 함께 담은 배포용 페이지다.
- `images\`: 해당 행사에서 사용하는 이미지 원본을 보관한다.
- `.github\skills\`: 작성 및 디자인 검토를 돕는 가이드다.
- `.github\workflows\static.yaml`: 별도의 GitHub Pages 배포 설정이다. 티켓타코 행사 소개를 자동으로 갱신하지 않는다.

## 로컬 실행과 작업 명령

별도 빌드, 패키지 설치, 번들러가 필요하지 않다. 저장소 루트에서 실행한다.

```powershell
npx --yes serve .
```

- 사용자가 서버를 이미 실행했다면 그 서버를 사용한다. 중복 서버나 Python 서버를 새로 띄우지 않는다.
- 기본 확인 주소: `http://localhost:3000/ticketaco/2026/12/dddseoul/`. 실제 주소와 포트는 실행 중인 서버를 따른다.
- 기존 빌드, 린트, 테스트 스크립트는 없다. 단순 HTML 작업을 위해 새 도구나 의존성을 추가하지 않는다.
- 변경 후 `git diff --check`를 실행한다.

## 티켓타코 HTML 작성 계약

사용자는 **`<body>` 태그 안쪽 내용만 복사**해서 티켓타코에 붙여넣는다.

- 배경과 전체 레이아웃 스타일은 `body`가 아니라 그 안쪽의 래퍼 `div`에 둔다.
- 복사할 모든 콘텐츠는 그 래퍼 안에 둔다. 필요한 스타일은 각 요소의 `style` 속성에 작성한다.
- `class` 속성을 사용하지 않는다. 클래스 선택자, 외부 CSS, CSS 변수, JavaScript에 의존하지 않는다.
- `head`의 제목, 메타데이터와 로컬 미리보기용 `body { margin: 0; }` 초기화는 유지할 수 있다. 복사한 콘텐츠가 이 설정에 의존해서는 안 된다.
- 별도의 메뉴, 고정 버튼, 전역 스타일로 티켓타코의 화면 구성을 덮어쓰지 않는다.
- 의미에 맞는 HTML 요소, 이미지 `alt`, 구분하기 쉬운 링크 문구를 사용한다.
- `id`와 ARIA 속성은 로컬 편집과 접근성을 위해 유지할 수 있지만, 배포 후에도 남는다고 가정하지 않는다.
- 외부 링크에는 HTTPS를 사용하고, 새 창 링크의 원본에는 `rel="noopener noreferrer"`를 유지한다.

### 실제 배포에서 관찰한 스타일 처리

아래 내용은 2026-09-09 티켓타코 배포 DOM과 로컬 원본을 비교한 결과다. 플랫폼이 바뀌면 다시 비교한다.

| 항목 | 티켓타코의 처리와 작성 규칙 |
| --- | --- |
| `display: flex`, `display: inline-flex`, `flex-wrap`, `align-items`, `justify-content`, `gap` | 유지한다. 행 배치와 줄바꿈에 사용할 수 있다. |
| `flex`, `flex-grow`, `flex-shrink`, `flex-basis` | 제거한다. 자식 요소의 너비는 명시적인 `width`로 지정한다. |
| `width`, `min-width`, `max-width`, `padding`, `margin`, 테두리, 배경색 | 유지한다. 인라인 레이아웃의 기준으로 사용한다. |
| 제목의 기본 글자 크기와 굵기 | 사이트 초기화 CSS가 상속값으로 바꾼다. 모든 제목에 `font-size`와 `font-weight`를 명시한다. |
| `box-sizing`, `color-scheme`, `-webkit-font-smoothing` | 인라인 값을 제거한다. 로컬 보조 설정으로 둘 수 있지만 배포 동작의 유일한 근거로 삼지 않는다. 관찰한 티켓타코 전역 CSS는 `border-box`를 적용한다. |
| `white-space`, `font-variant-numeric`, `text-underline-offset` | 원본 설정을 유지하지 않았다. 줄바꿈 방지나 정렬을 이 속성에 맡기지 않는다. |
| `id`, ARIA 속성, `datetime`, `rel` | 배포 DOM에서 제거했다. ID 기반 스타일, 앵커 이동, 스크립트 동작에 의존하지 않는다. |
| 이미지의 `src`, `alt`, 크기, `loading`과 링크의 `href`, `target` | 관찰한 배포에서 유지했다. 이미지 크기는 인라인 스타일에도 명시한다. |

이 표는 전체 허용 목록이 아니다. 처음 사용하는 속성이나 값은 실제 게시 결과와 비교한다.

## 레이아웃과 브랜드

- 포스터형 상단과 날짜·장소 카드를 유지하고, 하단의 CFP, 프로그램, 연사, 시간표를 독립적으로 확장한다.
- 기본 흐름은 행사 정보, CFP·참가 신청, DDD 소개, 프로그램, 오시는 길, 후원사, 행동 강령·지난 행사 순서다. 확정 정보와 사용자 요청에 맞춰 조정한다.
- DDD Seoul 공식 주황색 `#ba4609`, 흰색 `#ffffff`, 진한 글자색 `#212529`를 중심으로 구성한다.
- 보조 배경은 `#f8f5f2`, 테두리는 `#e5ded8`, 보조 글자색은 `#62615f`를 참고한다. 다른 행사 브랜드의 색상이나 그라데이션을 기본값으로 쓰지 않는다.
- `main`은 `width: 100%`를 사용하고 자체 고정 `max-width` 제한을 두지 않는다.
- 티켓타코의 상위 컨테이너가 최대 너비와 바깥 여백을 정한다. `100vw`, 음수 여백, 절대 위치로 티켓 구매 영역을 침범하지 않는다.
- 반응형 열 너비는 **뷰포트가 아니라 콘텐츠 컨테이너**를 기준으로 계산한다.
- 자식의 `flex` 대신 아래와 같은 `width` 패턴을 사용한다. 두 열의 너비와 `gap`의 합이 부모 너비를 넘지 않도록 맞춘다.

```html
<div style="box-sizing: border-box; display: flex; flex-wrap: wrap; gap: 20px;">
    <div style="box-sizing: border-box; width: min(100%, max(calc(50% - 10px), calc((600px - 100%) * 1000))); min-width: 0;">
        ...
    </div>
    <div style="box-sizing: border-box; width: min(100%, max(calc(50% - 10px), calc((600px - 100%) * 1000))); min-width: 0;">
        ...
    </div>
</div>
```

위 패턴은 부모 너비 약 600px 이상에서 두 열, 그 아래에서 한 열을 사용한다. `1000`은 전환 구간을 좁히는 계수다. 중단점 근처와 작은 화면도 확인한다.

## 글자 크기와 가독성

- `main` 한 곳에만 기준 `font-size: 18px`를 지정한다.
- 나머지 글자 크기는 모두 `em`으로 표현한다. 티켓타코의 `html`을 기준으로 삼는 `rem`이나 고정 `px`, 글자 크기용 `vw`를 사용하지 않는다.
- 본문은 기준 크기를 상속하거나 `1em`을 사용한다. 안내 문구는 `0.9em`, 배지·보조 정보는 `0.8em`을 기본값으로 삼는다.
- 중간 레이아웃 컨테이너에 축소 비율을 지정한 뒤 자식에서 다시 축소하지 않는다.
- 행사명과 연도, 시간과 KST, 장소명과 층수처럼 크기가 다른 텍스트는 기준 크기를 상속하는 부모 아래의 형제 요소로 배치한다.
- 제목은 상대 크기와 굵기를 모두 명시한다. 연도나 숫자가 중첩 비율 때문에 의도보다 작아지지 않도록 한다.
- 행간은 단위 없는 값으로 지정한다. 본문은 약 `1.7`에서 `1.95` 사이의 여유를 유지한다.
- 글자와 맞물리는 배지 너비도 상대 크기로 조정한다. 현재 CFP 배지는 `0.8em` 글자와 `8.5em` 너비를 쓰므로, 옆 열에서 기준 크기의 `6.8em`과 간격을 확보한다.
- 기준 크기를 바꿀 때 본문, 제목, 연도, 보조 정보가 모두 같은 비율로 따라가는지 확인한다.

## 이미지와 링크

- 다른 행사 디렉토리의 파일에 의존하지 않는다. 필요한 자산은 해당 DDD Seoul 행사의 `images\`에 보관한다.
- 배포용 HTML에서는 상대 이미지 경로 대신 다음 형식의 절대 주소를 사용한다.

```text
https://raw.githubusercontent.com/dddseoul/event-landing-page/refs/heads/main/ticketaco/{YYYY}/{MM}/{event-slug}/images/{filename}?v=1
```

- 공식 DDD Seoul 로고는 예외로 `https://dddseoul.kr/images/logo-dddseoul-whiteorange.png?v=1`을 유지한다.
- 이미지와 `og:image` 주소에는 버전 쿼리를 붙인다. 새 이미지는 `?v=1`로 시작하고, 같은 경로의 이미지 내용을 바꾸면 버전을 올린다.
- 원본 이미지를 `main`에 먼저 반영해야 위 raw URL로 접근할 수 있다. 사용자 요청 없이 커밋하거나 푸시하지 않는다.
- 이미지에 의미 있는 `alt`, 크기 또는 비율, 반응형 `max-width`를 지정한다. 지도는 원래 비율을 유지하고 로고는 `object-fit: contain`으로 처리한다.
- 로컬 이미지 원본과 출처 기록은 유지한다. 과거 자산의 출처 기록을 런타임 의존성과 혼동하지 않는다.
- 지난 행사 링크는 공식 연도별 주소를 사용한다: `https://dddseoul.kr/2024`, `https://dddseoul.kr/2025`.
- 저장소 이름만 보고 GitHub Pages 공개 주소를 추측하지 않는다. 조직 사이트의 사용자 지정 도메인이 프로젝트 사이트에도 영향을 줄 수 있다.

## 콘텐츠 작성 원칙

- 행사 날짜, 시간대, 장소, 주차, 발표자, 후원사, 신청 상태는 사용자 정보와 `index.md`를 기준으로 작성한다.
- 문구를 바꾸면 `index.md`와 `index.html`을 함께 수정한다. 메타 설명과 접근성 문구도 관련이 있으면 맞춘다.
- 확정하지 않은 세션, 시간표, 발표자, 연락처, 혜택, 신청 기한을 만들어 넣지 않는다. 필요한 항목은 준비 중 또는 추후 안내로 표시한다.
- 한국어 안내는 자연스러운 능동형을 사용한다. 불필요한 피동·수동 표현을 피한다.
- CFP와 참가 신청은 서로 다른 절차다. 한쪽 상태를 바꾸면서 다른 쪽 상태까지 바꾸지 않는다.
- 티켓 구매는 티켓타코 자체 UI를 사용한다. 우측 또는 아래의 참가신청 버튼을 안내하고, 별도의 구매 버튼이나 `href="#"` 자리표시자를 추가하지 않는다.
- CFP 링크가 없으면 오픈 예정 상태를 보여준다. 실제 신청 주소를 받은 후에만 링크를 추가한다.
- 확인한 후원사만 표시하고, 커뮤니티 행동 강령 링크 `https://dddseoul.kr/coc`를 유지한다.

## 확인과 작업 마무리

1. 기존 서버에서 해당 HTML과 이미지가 정상 응답하는지 확인한다.
2. 복사할 `body` 안쪽에 필요한 스타일이 모두 있는지 확인한다. 클래스, 스크립트, 자식의 `flex` 너비 설정을 새로 넣지 않았는지 살핀다.
3. 레이아웃이나 글자 크기를 바꿨다면 320px, 375px, 768px, 1280px, 1920px 화면과 좁은 본문 컨테이너를 비교한다.
4. 티켓타코와 비교할 때 같은 뷰포트뿐 아니라 **같은 본문 너비**도 맞춘다. 브라우저 전체 너비와 플랫폼이 할당한 너비를 혼동하지 않는다.
5. 실제 배포 DOM의 속성 제거와 전역 CSS를 확인한다. 단순히 로컬에서 잘 보인다는 이유만으로 게시 결과도 같다고 단정하지 않는다.
6. 가로 넘침, 겹침, 제목 굵기, 본문 가독성, 배지 줄바꿈, 이미지 표시, 링크 및 원본 문구를 확인한다.
7. 프로덕션 DOM의 임시 미리보기는 서버에 저장한 수정이 아니다. 임시 변경은 되돌리고, 실제 게시를 하지 않았다면 사용자에게 재복사·저장이 필요하다고 안내한다.
8. 사용자의 기존 수정과 무관한 파일은 건드리지 않는다. 변경 범위를 살핀 뒤 요청받은 경우에만 커밋·푸시·배포한다.
